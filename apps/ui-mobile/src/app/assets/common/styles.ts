import { StyleSheet } from 'react-native';

export const commonStyles = StyleSheet.create({
  defaultBorder: {
    borderRadius: 4,
    borderColor: '#aaaaaa',
    borderWidth: 1,
    // elevation: 5,
    // shadowColor: '#888888',
  },
});
